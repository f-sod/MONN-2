Before starting create your conda environnement with the right python version by usinf the .ylm file in the repository 
    - conda env create -f "environment."yml

Create a plip env in a different one (to avoid dependicies errors )
Use the plip.sh script to extract the interaction from all your pdb file

Check if some of the _ideal.pdb files are empty and print their id 
